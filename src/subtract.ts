/**
 * Subtracts one number from another.
 * @param first The number to subtract from.
 * @param second The amount to subtract.
 */
export default function subtract(first: number, second: number): number {
	return first - second;
}
